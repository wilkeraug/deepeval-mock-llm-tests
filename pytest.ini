[pytest]
markers =
    smoke: fast, critical-path checks
    coreflow: core conversational tool flows
    content: open-ended content/QA checks
    guardrail: safety and refusal checks
testpaths = tests
